machine:
  services:
    - docker
  python:
    version: 2.7

dependencies:
  override:
    - pip install pyvows coverage tornado_pyvows thumbor botornado py-dateutil

test:
  override:
    - pyvows -c -l thumbor_aws
